## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How to see it

<!-- Which screen, control, or command shows the change. -->

## Checklist

- [ ] `npm run build-booklet` was run after editing anything under `renderer/` (the generated `renderer/booklet.html` is committed and CI diffs it)
- [ ] `npm run check` passes
- [ ] `npm test` passes (or `npm run test:fast` plus a note on what was skipped)
- [ ] `npm run audit` passes
- [ ] No user state, keys, screenshots with private paths, or files from `data/` are included
